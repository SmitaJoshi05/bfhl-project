# Node modules (the obvious one people still mess up)
node_modules/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (don’t leak secrets like a rookie)
.env

# OS files (Windows + Mac junk)
.DS_Store
Thumbs.db

# IDE/editor files
.vscode/
.idea/

# Build / cache
dist/
build/
.cache/

# Optional: ignore frontend cache if using bundlers later
frontend/.cache/

# Optional: coverage reports
coverage/

# Package lock (optional — keep if you want reproducibility)
# package-lock.json